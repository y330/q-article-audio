<script>
  // components
  // variables
  export let currentUrl;
  class GetContent {
    /*  async function getContent(url) {
          return extractor(url);
        } */
    /*  getContent(tabs[0].url).then(function (value) {
          insertContent(value);
        }); */
  }
</script>

<section class="tabpane__">
  <span>Right now, browsing <a href="#">{currentUrl}</a>!</span>
</section>

<style>
</style>
